# Credentials — never commit real tokens
config.txt
.env
ngrok_do_bot.yml
# .env.example is safe to commit (no real values)

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Runtime state files
data/daily_assignments.json
daily_assignments.json

# Large data files — store on server or object storage
data/ZSDOROUTEWRH.xlsx
data/ZSDOROUTEWRH-bot.xlsx

# OS
.DS_Store
Thumbs.db

# Log files (keep the directory via logs/.gitkeep)
logs/*.log
