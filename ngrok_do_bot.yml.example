version: "2"
authtoken: YOUR_NGROK_AUTHTOKEN_HERE
tunnels:
  do_bot:
    proto: http
    addr: 5000
    # ngrok v3+: use "domain:" instead of the old "hostname:"
    domain: degraded-sincerity-glue.ngrok-free.dev
